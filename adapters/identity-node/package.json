{
  "name": "@art-pollinator/identity-node",
  "version": "0.0.0",
  "description": "IdentityPort and SignatureVerifierPort implementations for a Node environment: Ed25519 keypair generation, secure filesystem storage, and signature verification (issue #57/#58).",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@art-pollinator/core": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5"
  }
}
